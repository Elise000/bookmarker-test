

<table>
  <tr>
    <th>Url</th>
    <th>User Id</th>
  </tr>

<% @bookmarks.each do |bookmark| %>
<tr>
 <td><%= bookmark.url %></td>
 <td><%= bookmark.user_id %></td>
</tr>
<% end %>
</table>

<%= link_to 'Add bookmark', new_bookmark_path %>

